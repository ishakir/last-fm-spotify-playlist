var _ = require('find_unique');
var assert = require('assert');

describe("The find unique method") {
	it("should reject a remix if the original song entered wasn't a remix") {
		var song = {name: "Trick of the Tail"};
		var songs = [
			{name: "Trick of the Tail", id: 1}
			{name: "Trick of the Tail - Awesome Remix", id: 2}
		];
		assert.equal()
	}
}